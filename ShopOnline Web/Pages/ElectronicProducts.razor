@page "/ProductCategory/3"

<PageTitle>Electronics - ShopOnline.Web</PageTitle>

@if (Electronics == null && ErrorMessage == null)
{
    <DisplayLoading LoadingMessage="Loading Electronics" />
}
else if (ErrorMessage != null)
{
    <DisplayError ErrorMessage="@ErrorMessage" />
}
else
{
    <DisplayItemsInCategory Header="Electronics" Products="@Electronics" />
}

@code { }
